node_modules/
dist/
.env
.env.*
!.env.example
coverage/
*.log

# Keep the bundled evaluation fixture (including its node_modules noise, which
# the tools are expected to skip at runtime).
!eval/fixtures/
!eval/fixtures/**
